{
  "watch": ["src", ".env"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts", "node_modules", "dist", "coverage", "*.log"],
  "exec": "ts-node -r tsconfig-paths/register src/main.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
